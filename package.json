{
  "name": "mywranglerapp",
  "version": "0.0.0",
  "devDependencies": {
    "jest": "^29.0.3",
    "worktop.build": "^0.0.5",
    "wrangler": "2.1.6"
  },
  "private": true,
  "scripts": {
    "build": "worktop build ./src/index.js",
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "jest"
  },
  "dependencies": {
    "faunadb": "^4.7.0",
    "worktop": "^0.7.3"
  }
}
